package com.sentinel.common.domain.enums;

/**
 * Severity levels for security events.
 * Replaces magic strings for type-safety and consistency.
 */
public enum Severity {
    INFO,
    WARNING,
    CRITICAL
}
